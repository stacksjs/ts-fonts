export type {
  AxisInput,
  BuildVariableFontOptions,
  MasterInput,
  NamedInstanceInput,
} from './build'
export { buildVariableFont } from './build'
export {
  applyAvarMap,
  applyGvarToGlyph,
  createInstance,
  isVariableFont,
  listAxes,
  listNamedInstances,
  normalizeAxisValue,
  normalizeCoordinates,
  normalizedCoordsArray,
} from './instance'
